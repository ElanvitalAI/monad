export const GPT_CHAT_VARIANT = `## Provider variant: gpt

- Prefer the smallest correct change that satisfies the request.
- Batch independent tool work in parallel where the runtime supports it.
- Keep explanations compact and lead with the concrete result or next action.
`;
