---
id: req:v1:d59d1b060372070c
intent: "매일 정오 이 저장소의 열린 판 수를 세어 한 줄로 보고한다"
schedule: "0 12 * * *"
requires: [report.openprcount]
---

# 정오 열린 판 수 리포트 (🅢 142차 · «자식이 어디에 쓰나» 시험대)

> ⚠️ 판정은 위 프론트매터«만» 읽는다.

이 요청은 `#14898`(생성기를 `dev --ask` 로) 이 «실제로» 사람 트리를 안 건드리는지 재려고 넣었다.
앞 요청(`req-v1-10a9d92906d400c4`)을 쏠 때는 자식이 `--test dev --implement` 로 떠서
***사람 트리에 직접 썼고*** 그 뒤 `git pull` 이 실패했다. 그 수리가 먹었는지 이 요청으로 잰다.
