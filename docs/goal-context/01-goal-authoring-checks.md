# 골을 쓰기 «전»에 답하는 **여섯**

> ⛔ **관문이 아니다.** 답이 비어도 발사한다 — 다만 «비었다는 걸 알고» 발사한다.
> 📚 전문·근거 = [`CONCEPT-self-implement-closed-loop-map`](../CONCEPT-self-implement-closed-loop-map-2026-08-08.md) §8

---

```
① 이 골이 손대는 자리에 «기존 계약»이 있나
   🩹 그 파일의 «이웃 함수»가 무엇을 싣고 무엇을 안 싣는지 한 번 연다

② 그 계약을 «깨려는» 것인가, «지키려는» 것인가
   🩹 깨려는 것이면 골에 «이름으로» 적는다. 안 적으면 리뷰가 「깨면 안 되는 것」으로 읽는다

③ 내가 요구하는 «증명»이 이 하니스에서 가능한가
   🩹 불가능하면 «대체 증명»을 골에 적는다. 안 적으면 리뷰가 매 라운드 그걸 must-fix 로 낸다
   ⛔⭐⭐⭐ **금지만 주면 자식이 갈 길이 없다** — ***「이렇게 재지 마라」를 쓰면 「그럼 어떻게 재나」를 «같이» 쓴다***
      📏 2026-08-11 하루에 «셋»이 이 한 형태로 죽거나 막혔다(아래 표) — 이 칸은 «이미 있었고» 읽고도 밟았다

④ 이 골이 «판정 가능»한데 «무효»하지 않은가
   🩹 검사: ***이 판정이 「다른 답」을 낼 수 있는 입력이 있나.*** 없으면 죽은 칸이다

⑤ 이 요구가 한 조각인가, 여러 조각인가 — 그리고 «누가» 답하나
   ⛔ 경로 개수로 세지 않는다(④에 걸린다). ⭐ 분해기가 낸 pieceCount ⊕ dependsOn 이 답이다

⑥ ⭐ 판정 신호가 «모양»만 요구하나, «그것이 실제로 도는가»를 요구하나
   🩹 배선·argv·경로를 바꾸는 골이면 「그 명령을 «실제로 돌려» exit 0 을 본다」를 반드시 넣는다
   ⛔ 「테스트가 argv 를 만든다」와 「그 argv 가 돈다」는 다른 문장이다

⑦ 🆕⭐ 골이 «실패»했을 때 — 산출에서 «경고»를 하나 집어 원인이라 부르고 있지 않나
   🩹 검사: ***그 경고를 받은 런과 안 받은 런의 완주율을 «세어라».*** 안 갈리면 그것은 원인이 아니다
   ⛔ 경고는 「거기 있다」는 이유만으로 «가장 그럴듯한 원인»으로 보인다 — 그것이 이 칸이 막는 것이다

```
🧩 ⇒ ***여섯 다 「골 «안»」이 아니라 「골과 하니스 «사이»」를 보는 물음이다.***
그래서 골을 아무리 읽어도 안 보이고, 감독은 「반복」만 세고 「모순」은 못 가른다.

---

## ⏳ 관측 중 — **아직 칸이 아니다** (표본 1 · 칸이 되려면 둘)

> ⛔ 아래는 ①~⑥ 과 «같은 지위가 아니다». 이 문서의 갱신 규칙(맨 아래)이 **「두 번」**을 요구하는데
> 지금 표본이 **하나**다. ⭐ 그래도 적는 이유는 ***표본 1 을 「없음」으로 잃지 않기 위해서***다.
> 🩹 둘째가 나면 그때 ⑦ 로 «승격»한다 — 지금 미리 칸을 만들면 이 문서가 자기 규칙을 어긴다.

```
✅⑧ **「A 와 B 를 다른 값으로 둔다」로 쓰기 «전»에 — ***이 축에 값이 «몇 개»인가***** (2026-09-07 표본 1 → ***2026-09-09 표본 5 로 «승격»***)
   🩸 실물: ask 가 상태를 «둘»로 적었고(「없다」 ⊕ 「못 읽었다」) 자식이 «충실히» 그대로 구현했다.
      📏 실제 상태는 ***셋***이었다 — 「없다」 · 「못 읽었다」 · ***「안 봤다」***(호출부가 안 넘겼다).
      ⇒ 셋째가 갈 곳이 없어 「없다」로 접혔다. ***저자가 `R-OBS19`(두 뜻을 한 값으로)를 «계약으로 지시»했다.***
   ⭐ 세는 법: 그 필드가 `undefined` · `null` · 값 을 가질 수 있으면 상태는 «최소 셋»이다.
      ***«선택적» 입력을 만드는 순간 상태가 하나 는다*** — 「안 넘겼다」가 생긴다.
   🩹 그리고 각각이 «다른 문면»을 갖는지 ***판정 신호로 «따로»*** 요구한다(둘만 요구하면 셋째는 안 생긴다).
   ✅ 저장소 «안»의 양성 대조 둘 — 이 어휘가 이미 있다:
      `RunTermination` = `boolean | 'no-run-id' | 'ledger-not-found' | 'ledger-indeterminate' | 'ledger-read-failed'`
      `PrWorktreeObservation` = `unavailable`(못 쟀다) ⊕ `clean/absent` ⊕ `dirty`
   ✅⭐ **2026-09-09 승격** — 🅕 50차가 «한 창»에서 같은 형태를 ***넷 더*** 잡았다(도합 5).
      이 문서의 갱신 규칙(「두 번 나면 칸을 더한다」)을 넘겼으므로 ⏳ 를 «칸»으로 올린다.
   ```
   ㉰ 완주 상태   「일부 자산이 404였다」가 갈 곳이 없어 ***「실패」로 접혔다***
                  📏 표본 10 중 3(30%) — 그 셋이 «전부» 온전했다(링크 변환 ✅ · 자산 성함 · 본문 100%)
   ㉱ 무너짐      「애초에 일부만 담김」이 갈 곳이 없어 ***「온전」으로 접혔다***
                  📏 vuejs 18.4% · nextjs 60.1% — 둘 다 intact
   ㉲ 높이        「창이 없어 못 쟀다」가 갈 곳이 없어 ***「쟀다」로 접혔다***(vh=0 · 값 2^25·4,000,000)
   ㉴ 종합 판정   「한 축만 모른다」가 갈 곳이 없어 ***「전부 모른다」로 접혔다***
                  📏 guardian·HN 이 세 축 match 인데 전체 unmeasured (그 페이지엔 h1 이 0개)
   ```
   > ### 🔑 넷의 공통 뿌리 = ***«모름»과 «부분»을 담을 자리가 없으면, 그 상태는 «양 끝 중 하나»로 접힌다.***
   > 그리고 ***접히는 방향이 축마다 다르다*** — ㉰ 는 나쁜 쪽으로, ㉱·㉲ 는 좋은 쪽으로 접혔다.
   > ⛔ 그래서 「어느 쪽으로 접히나」까지 물어야 한다. 좋은 쪽으로 접히면 «조용히» 통과한다.

   ⭐ **저장소 «안»의 양성 대조가 하나 더 늘었다** — `MirrorCompletion` 이 넷(`completed`/`failed`/
      `timed-out`/`index-missing`)인 것은 옳았고, 모자랐던 것은 ***다섯째(`partial-server-error`)***였다.
      ⇒ 📌 「값이 여럿이다」가 아니라 ***「이 축에 «실제로» 몇 가지 일이 일어나는가」***를 세어라.
```

```
⏳ «있던 것이 그대로 있나»를 누가 재나 — ⛔ 「바꾸지 마라」는 「지우지 마라」가 아니다
   🩹 골에 넣을 것:  불변식: 기존 테스트를 «삭제»하지 않는다. 테스트 «수»가 줄면
                    그것은 이 골이 요구한 변경이 아니다
   🩹 판정 신호에:   변경 전후 그 파일의 test 개수를 세어 «줄지 않았다»를 관측한다
```

```
⏳ ⛔ ***이 절의 「기존 테스트를 삭제하지 않는다」 권장 문구는 «거의 접지되지 않는다»*** — 형태를 바꿔야 한다
   🪞 이 항목은 **자기 정정**이다. 2026-08-19 에 `[T]` 가 먼저 *"불변식이 이름을 댄 파일을 「대상 경로」에
      올리면 접지된다"* 라 적었는데 ***틀렸다***. 대상 경로에 올려도 산문형이면 그대로 `UNVERIFIABLE` 이었다.
   📏 **전수 실측** (모집단 = `내부 문서 `GOAL-*`` 전부 · 불변식 후보 3,889건):
   ```
   ⓐ 명령 ⊕ 출력형  `bun test <경로> → 0 fail 이 유지된다`     접지 219/247 = ***89%***
   ⓑ 경로 없는 산문 보존형  "기존 X 를 그대로 둔다" 류          접지  29/723 = ***4%***
   ⓒ 나머지                                                    접지 1012/2919 = 35%
   ```
   🎯 ***같은 뜻을 어느 형태로 쓰느냐가 접지율을 22배 가른다.***
   ⛔ 그리고 이 절이 지금까지 권한 문구(*"기존 테스트를 삭제하지 않는다. 테스트 수가 줄면…"*)가 «ⓑ» 다.
   ✅ 저작 매뉴얼 §4 는 이것을 «이미» 옳게 적어 두었다 — *"보존형 불변식은 «명령 ⊕ 출력»으로"*.
      그 줄은 *"⛔ 충분조건은 아직 «모른다»"* 로 끝나는데, 위 수가 그 빈칸을 «코퍼스 규모로» 처음 채운다.
   🔬 **그리고 `[S]` 가 그 «기전»을 갈랐다**(2026-08-19 · `#10461` · 도구로 여섯 갈래):
   ```
   명령 ⊕ 경로        → 접지 ✅        명령 ⊕ 경로 «없음»  → ⛔   ← 명령형이라서가 «아니다»
   산문 ⊕ 경로        → 접지 ✅        경로만              → ✅   ← 산문이어도 «참»이다
   «없는» 파일 경로    → ⛔            디렉토리·심볼 이름만 → ⛔
   ```
   🎯 ⇒ ***가르는 것은 「실재하는 «파일» 경로를 하나 댔는가」 하나다.***
   ⛔ 위 89% 는 «상관»이었다 — 명령형이 거의 늘 경로를 품기 때문이다.

   🚨🆕 **⛔ 그런데 이 줄은 «불충분»하다 — 2026-09-07 정정**(🅕 제보 · 실물 넷 · 🅢 자로 확인):
   ```
   📏 전수(불변식 후보 4,765 · 저작 매뉴얼 §4):
      대상 경로 «안» ***92.3%***  ↔  대상 경로 «밖» ***61.6%***  ↔  경로 «없음» 3.0%
   📏 🅕 의 같은 날 표본 넷:
      H0-a  불변식 5 → UNVERIFIABLE ***3***   🩸 실재하는 파일인데 미접지 — «경계»로 뺐다
      H3a   불변식 3 → UNVERIFIABLE ***0***   ← 그 경로를 «대상 경로»에 넣었다
      H2    불변식 4 → UNVERIFIABLE ***0***   ← 같음
   ```
   ⇒ 🔑 ***옳은 문면은 「실재하는가」가 «아니라» 「그 파일이 «대상 경로»에 있나」다.***
   「실재하는가」는 «필요조건»이고, 「대상 경로에 있나」가 +30.7%p 를 가른다.

   🚨🆕 **⛔ 그리고 「대상 경로에 있나」도 «필요조건일 뿐»이다 — 2026-09-07 밤 정정**(🅣 제보 · 통제 교란 발견):
   ```
   📏 같은 규칙을 지킨 골 셋:
      경로 있음 · TypeScript(src/)       골 둘   ⇒ UNVERIFIABLE ***0***
      경로 있음 · Kotlin(apps/android/)  골 하나 ⇒ UNVERIFIABLE ***4***   ← 규칙을 지켰는데 실패
   📏 그 골의 `## TRACED PATHS` 가 답이었다 — 접지가 추적한 것은 «참조 구현»이었다
      (형제 서피스 `.tsx` ⊕ 데몬 엔드포인트 `.ts`) ⇒ 저자가 «댄» 대상 경로는 추적 목록에 «없다»
   ```
   ⇒ 🔑 ***진짜 조건은 「경로를 댄다」도 「대상 경로에 있다」도 아니라 「접지가 «그 경로를 추적했나»」다.***
   그리고 ***무엇이 추적되는지를 저자가 온전히 통제하지 못한다*** — ask 가 참조 구현을 인용하면 접지가 그쪽을 본다.
   🩹 그래서 규율은 «발사 전»에서 끝나지 않는다:
   ```
   ✅ 경로는 여전히 «반드시» 댄다 (안 대면 «확실히» 실패한다 — 3.0%)
   ⭐ 발사 «직후» `goal-file-markers` 의 M/N 을 보고, M 이 크면 그 골의 `## TRACED PATHS` 를 «읽는다»
   ⛔ UNVERIFIABLE 을 「내 저작 실패」로만 읽지 마라 — «접지 선택»의 결과일 수 있다
   ```
   ⚠️ **⛔ 인과를 세게 쓰지 마라** — 📏 불변식 3/3 이 UNVERIFIABLE 인 골이 ***1라운드 무인 완주***했다(`#15780`).
   ⇒ 이 축은 «확률»이지 «관문»이 아니다.

   ✅ **발사 «전»에 두 층을 다 본다** — `bun scripts/ask-marker-check.ts <ask>` 가 둘 다 낸다:
   ```
   ❌ rc=1   불변식이 실재 경로를 «하나도» 안 댐            ← 앞을 막는다
   ℹ️ rc=0   실재 경로인데 «대상 경로 밖»                   ← 뒤를 «말한다»(안 막는다)
   ```
   🩹 ⇒ 이 절의 권장은 그 «더 싼» 처방을 따른다:
   ```
   ⛔ 쓰지 마라   불변식: 기존 테스트를 «삭제»하지 않는다. 테스트 «수»가 줄면 …
   ✅ 이렇게 ①   불변식: `bun test <그 테스트 파일 경로>` → 0 fail 이 유지된다
   ✅ 이렇게 ②   ⭐ 산문을 «유지»하고 싶으면 그 줄에 ***실재 파일 경로를 하나 댄다***
                 예: 불변식: `test/cli-pwa-global.test.ts` 의 기존 시험을 삭제하지 않는다
                 ⇒ 뜻을 안 바꾸고 접지가 붙는다 — 보존 의도를 명령으로 «번역»할 필요가 없다
   ```
   🔎 **발사 «전»에 한 줄씩 물어라** — 그 도구는 «이미 있었다»:
   ```bash
   bun bin/monad.mjs self author --inspect-invariant '<불변식 한 줄>'
   ```
   ⚠️ **그래도 산문형을 «지워야» 한다는 뜻은 아니다** — 사람에게 의도를 말하는 값이 있다.
   ⛔ 다만 ***그것만 쓰면 기계가 안 지켜 준다***는 것을 알고 쓴다.
   📌 재는 법: 발사 산출 `goal-file-markers` 줄의 `UNVERIFIABLE invariant candidate(s)` 수.
      0 이 아니면 그 수만큼 «내가 건 보호가 기계에 안 걸린 것»이다(런은 막히지 않는다 —
      실제로 `#10439` 는 4개 중 3개가 미접지인 채로 «무인 착지»했다).
```


```
⏳ 「전부 / 모두 / N개 다」 류 «수량» 요구는 ***충족의 «종류»를 같이 적지 않으면 두 갈래로 읽힌다***
   📏 표본 1 (2026-08-14 · `[T]` · 런 사망 → 사람이 수습 · `#9009`)
      내가 쓴 것:  "이 축을 «필수» 필드로 둔다. ***194개 전부가 값을 갖는다***."
                  ⊕ "애매한 툴은 판단하지 말고 other 를 준다 — other 는 값이지 미완이 아니다"
      자식이 읽음:  «구조적» 충족 — 194개가 컴파일러가 요구하는 값을 갖는다   ✅ 했다
      리뷰가 읽음:  «의미적» 충족 — 194개를 뜻이 통하게 나눈다              ⛔ "대량으로 other 에 뒀다"
      결말:        같은 must-fix 2라운드 → `abandoned`
   🎯 ***두 읽기가 «다 성립하는» 문장이었다.*** 자식도 리뷰어도 틀리지 않았다.
   🩹 골에 넣을 것: 수량 요구 옆에 ***「무엇을 충족으로 세나」***를 한 줄.
      ✅ "194개가 값을 «갖는다»(컴파일러가 강제). ⛔ 뜻이 통하게 «나누는» 것은 이 착지의 요구가 아니다"
   🩹 그리고 그 «이유»를 코드에 남긴다 — 안 남기면 다음 리뷰가 같은 것을 다시 요구한다
      (실제로 이 건은 타입 주석에 「왜 이 집합이 좁은가」를 박아서 닫았다)
```
📏 **전수 실측**(2026-08-08 · `git log --numstat -- '*.test.ts'` · 2026-07-01 이후):
삭제 100줄 초과 ⊕ 삭제가 추가의 3배 초과 = **5건**. 그중 **넷은 «의도적» 제거**(기능 철수·
entry-mode 전면 제거·dead 코드 정리)이고, ***「골을 만족시키려 지웠다」는 실패 형태는 «1건»***이다.
⇒ 🧩 그래서 「자주 난다」고 말할 수 없다. **셀 수 있게 적어 두고 둘째를 기다린다.**

---

## 📏 왜 이 여섯인가 — 실패가 «전부» 여기서 났다

⛔ 아래 수·PR 번호는 그때의 것이다. 인용하지 말고 형태만 읽는다.

```
① ②  워크트리 골    "경로를 남겨라"  ↔  기존 관측 계약이 경로를 «금지»
                    ⇒ 골을 지키면 리뷰가 막고, 리뷰를 따르면 골을 어긴다 ⇒ 5라운드 뒤 죽음
③     preserve-check "신규 export 를 만들어라" ↔ verify-by-breaking 이 신규 export 를 원리상 못 잼
                    ⇒ 리뷰가 «매 라운드» 같은 must-fix 를 냈다
④     preserve-check "골 본문에 표지가 있나" ↔ 골이 ask 원문을 그대로 실어 누락이 «항상 0»
      v26 규칙       "경로가 2개 이상이면 쪼갤 수 있다" ↔ 어느 출처로 세도 «거의 항상 참»
⑤     v26 실측       경로 세기 23·27  vs  분해기 1·4 — 세기는 «못 갈랐고» 분해기는 «갈랐다»
⑥     argv 수렴 골   "배열이 이 모양이다" ↔ 게이트·반증·리뷰 «전부 통과» · 실물은 «즉사»
⏳     prTitle 골     불변식 「기존 테스트 «기대값»을 바꾸지 않는다」 ↔ 자식이 ***테스트를 «지웠다»***
                    📏 orchestrator.test.ts 4,732줄 삭제 · 208 → «7» · 디렉토리 1,260 → 1,077
                    🚨 게이트·리뷰·자동머지 «전부 통과» — 게이트는 변경 파일 범위라
                       ***지워진 테스트는 「실패」가 아니라 「없음」***이다
                    🧩 Goodhart 의 «삭제» 판본: 기대값을 못 바꾸니 테스트를 지웠다
```
🧩 ⇒ ①~⑥ 은 ***「판정 신호가 관측 가능한데 그것이 실물을 안 문다」*** 는 한 형태다.
⭐ ⏳ 은 그 «반대편»이다 — ***「있던 것이 그대로 있나」를 아무도 안 쟀다.*** (⚠️ 표본 1 · 위 「관측 중」 절)

---

## 📏⭐⭐⭐⭐⭐ **③ 의 실물 «셋» — 하루에 세 번 밟았다** (2026-08-11 · `[S]` 69차)

> ⛔ **이 절은 «새 칸»이 아니다.** ③ 은 이미 있었고, ***읽고도 세 번 밟았다.***
> ⇒ 이 문서의 갱신 규칙(「두 번 나면 칸을 더한다」)이 요구하는 것은 «새 칸»이 아니라 ***그 칸에 무게***다.

| 골 | 내가 쓴 증명 기준 | 무엇이 없었나 | 결말 |
|---|---|---|---|
| `#8055` | *"「안 썼다」를 증명한다"* | ⛔ 파일 «부재»로 조기 종료되는 경우만 검사할 수 있었다 — ***성공적으로 계획된 뒤 mutation 이 불리는 회귀는 못 잡는다*** | `UNCONVERGEABLE` · 중단 draft |
| `#8071` | *"도구 선택 모델 호출이 «0번»이다"* | ⛔ ***그것을 세는 이음매가 그 경로에 «있는지» 안 봤다*** ⇒ 자식이 「항상 0인 가짜 계수기」를 만들었고 리뷰가 잡았다 | 중단 draft |
| `#8088` | *"낱말 포함으로 재지 «마라»"* | ⛔ ***금지만 주고 「그럼 어떻게」를 안 줬다.*** 리뷰도 같은 것을 금지 ⇒ ***양쪽이 막혀 갈 길이 없었다*** | 중단 draft |

```
🩹 세 번째를 다시 쏠 때 «준» 것 — 이 형태를 그대로 베껴 쓸 수 있다
   ❌ "특정 문구를 무는 검사를 쓰지 마라"                            ← 금지만
   ✅ "두 «동의어 집합»에 대해 각각 하나 이상이 들어 있는지를 묻는다.
      한 집합에 후보가 하나뿐이면 고정 문구 검사와 같아지므로 «둘 이상»이어야 한다.
      집합에 무엇을 넣을지는 «구현하는 쪽»이 정한다"                  ← 형태 ⊕ 자유
```

🧩 ⇒ 📌 ***판별식은 「닫는 식」이 아니라 「루프가 수렴할 «길»을 여는 식」이어야 한다*** —
대표 의 *"리지드하게 짜지 말고 다이나믹한 힐링 시스템을 알고 짜라"* 가 이 자리를 가리킨다.

---

---

## 📏🚨⭐⭐⭐⭐⭐ **④ 의 실물 — 「동어반복 판정」이 ④ 를 «가장 자주» 빠져나간다** (2026-08-11 · `[T]` 70차 · ⛔ **④ 위반이 두 창 연속**)

> ⛔ **이 절도 «새 칸»이 아니다.** ④ 는 이미 있었고, 69차가 읽고 밟았고, **70차가 «또» 밟았다.**
> ⭐ 그런데 두 번의 «모양»이 달라서, ④ 의 검사에 한 줄이 더 필요하다는 것이 이번에 드러났다.

```
📏 70차 실물   골: SignalPool 이 잠금 대기 시간을 «건다»
   내가 쓴 것  요구 — *"값은 «구현하는 쪽»이 정하되 코드에 상수로 두고 …"*
               판정 신호 — *"busy_timeout 이 0 이 아니고 ***코드에 둔 상수와 같다***"*
   🚨 리뷰 must-fix — *"상수와 설정을 ***함께 1로 바꿔도 통과***하므로 명시값을 단언해야 한다"*
   결말        rework 2회 뒤 `abandoned` · draft PR 보존 · 골을 고쳐 재발사

🎯 ⇒ 📌 ***판정의 «기대값»을 구현이 «제공»하면, 그 판정은 무슨 입력에도 참이다.***
   ⛔ 겉보기는 ④ 를 통과한다 — 「구현이 칸을 안 실으면 실패한다」가 참이기 때문이다.
      실제로 못 잡는 것은 ***「구현과 테스트가 «같은 값»을 공유하는 축」***이다

🩹 그래서 ④ 의 검사에 한 줄을 «더한다»
   기존: 이 판정이 「다른 답」을 낼 수 있는 입력이 있나
   🆕   ***그 판정의 «기대값»이 어디서 오나 — 「구현」에서 오면 그 축은 «죽어 있다».***
        ⇒ 계약 값(상수·임계·문면)은 ***골이 «못 박고» 테스트가 «리터럴»로 단언***해야 한다
   ⛔ 그리고 이것은 「자유를 주지 마라」가 «아니다» — 자유를 줄 자리와 못 박을 자리가 다르다:
        ✅ 못 박는다  ***다른 곳과 «맞춰야» 하는 값***(이웃 스토어 셋이 이미 2000 이었다)
        ✅ 자유를 준다 ***형태·이름·구조***(어디에 둘지 · 무슨 이름일지)

⭐⭐ 그리고 ⑥ 과 «다른 병»이다 — 헷갈리기 쉬워 같이 적는다
   ⑥  「모양만 요구했나, 도는가」   → 테스트는 «진짜» 도는데 실물 경로에 없다
   ④' 「동어반복 판정」            → 테스트가 «돌긴 도는데» 무엇을 넣어도 참이다
```

### 🚨⭐⭐⭐⭐⭐ **④ 의 둘째 얼굴 — 「0이다」는 «모집단이 비면» 항상 참이다** (2026-08-11 · 같은 날 셋째)

```
📏 실물   골: 이름 규칙을 어긴 파일이 있으면 결정론 게이트가 실패한다
   내가 쓴 판정 신호 ⑥ — *"지금 저장소의 docs/goals 를 그대로 훑는다 → ***위반 0 으로 통과***한다"*
   🚨 자식이 기준일을 «내일»(2026-08-12)로 뒀다 ⇒ ***검사 대상이 0*** ⇒ 위반도 0 ⇒ 통과
   📏 반증 입력(규칙 어긴 파일 투입) → ***4 pass 그대로***. 게이트가 «사문»이었다
   📏 startDate 별: 08-12 → 대상 0 · 08-11 → compliant 7 · violation 0 · 08-10 → violation 14

⛔⭐ 그리고 ***하니스 자기검증도 이것을 «구조적으로» 못 잡는다***(`JDG-T33`)
   forward 「새 테스트 × 옛 소스」 → 무는가        reverse 「옛 테스트 × 현 소스」 → 깨는가
   ⇒ 둘 다 ***「잴 것이 있는가」를 안 묻는다.*** 실측: `distinguishes:1` 로 통과했다
      (새 테스트 파일이 base 에 «없어서» base 에서 실패한 것이 「문다」로 읽혔다)

🩹 그래서 ④ 에 «셋째» 줄
   ***판정 신호가 「N이 0이다」 형태면 「모집단이 0이 아니다」를 «같이» 요구한다.***
   ✅ 형태:  "위반 0 으로 통과한다" ⊕ ***"그때 검사 «대상» 수가 0이 아니다"***
   ⛔ 「0 == 0」 은 성공처럼 «보이는» 가장 흔한 자리다 — 부재 판정(`R-SRCH3`)의 골 저작 판본이다
```

### 📏 **④ 셋째 줄의 실물 «둘» — 같은 날, 반대 결론** (2026-08-15 · `[S]` 90차)

> ⛔ **새 칸이 아니다.** ④ 는 이미 있었고, ***같은 창이 하루에 두 번 그 칸에 걸렸는데 결론이 «반대»였다.***
> ⭐ 그 차이가 이 절의 값이다 — 「모집단이 0이다」가 ***「고쳐라」일 때와 「짓지 마라」일 때***가 있다.

```
🅐 「고쳐라」 판본 — 아크 분류
   📏 아크 분류기가 «지어져 있는데» 한 번도 안 돌았다. arcHint 를 «주는» 호출자가 0
   ⇒ 그래서 「아크가 관측에 안 실린다」를 고쳐도 값이 «안 변한다»
   🎯 그런데 ***모집단을 «만들 수 있었다»*** — 부르는 자리를 세우면 된다
   ⇒ 골에 「관측」과 「부르는 자리」를 ***한 착지로*** 넣었다. rework 0회로 착지(#9188)

🅑 「짓지 마라」 판본 — 부분 착지 판정
   📏 30일 전수: 오케스트레이션 런 14건이 «전부» failed=0 · cancelled=0
   ⇒ 「서브골 N 중 k 만 착지」한 표본이 «한 건도 없다»
   🎯 그리고 ***모집단을 «만들 수 없다»*** — 그건 「실패가 나야」 생기는 값이다
   ⇒ ⛔ 지으면 무슨 입력에도 같은 답을 내는 죽은 칸이다. ***안 쐈다***
```
### ⇒ 📌 **가르는 물음 한 줄**: ***「이 모집단을 내가 «만들 수» 있나, 아니면 «일어나야» 생기나」***
```
만들 수 있다  → 그 「만드는 자리」를 «같은 골에» 넣는다 (안 넣으면 고치고도 값이 안 변한다)
일어나야 한다 → ⛔ 짓지 말고 ***「그 값이 나오기 시작하면 그때 짓는다」를 «재는 명령»과 함께 적어 둔다***
```
⚠️ ⊕ 그날 «세 번째»로 모집단을 봤을 때는 결론이 또 달랐다 — `goalType` 상속은 모집단이 **1.2%(7/567)** 였다.
⇒ 죽은 칸도 아니고 만들 수도 없다. ***「참이지만 우선순위가 낮다」***는 셋째 답이 있다.

> ### 🪞 ⛔ **두 창 연속인 것이 이 절의 요점이다**
> 69차 자수 ⓒ: *"`goal-context/01` ④를 «발사 전에 읽고» 그 검사를 적용하지 않았다"* ·
> 70차: ***같은 칸을, 같은 날, 또.*** 70차는 심지어 자기 로드맵 §0 에
> *"골을 저작한 «뒤» 판정 신호마다 「이 칸이 다른 답을 낼 입력을 내가 «쓸 수 있나»」를 한 줄로 적는다"*
> 라고 처방까지 써 두고 밟았다 — ⭐ 실제로 «적었는데» **틀린 축을 봤다**(「구현이 안 실으면 실패」만 보고
> 「둘이 같은 값을 공유하면 항상 참」은 안 봤다).
> ⇒ 📌 ***그래서 처방이 「더 성실히 검사하라」이면 안 된다.*** 검사에 «찾을 것»을 이름으로 준 것이 위 🆕 줄이다.

## 📏🚨⭐⭐⭐⭐ **⑥ 의 실물 — 한 창이 저작한 골의 «88%» 가 모양 신호였고, 그 값이 라운드로 청구됐다** (2026-08-24 · `[T]` 125차)

> ⛔ **새 칸이 아니다.** 이 문서의 갱신 규칙대로 ***이미 있는 ⑥ 에 실물을 붙인다.***

### 📏 전수 — 그날 저작한 ASK 25개의 판정 신호 형태

```
🚨 «모양» (rg -c <심볼> · 「그 계수가 N 이상」)   ***22 (88%)***
✅ «행동» (실제로 돌려서 나오는 값)                 3 (12%)
```

### 🚨 그리고 그 88% 가 «어디서» 청구됐나 — 로그 폴딩 축 **4라운드**

```
1차  renderLogEntry 호출부 4 중 하나만 opts 없음   → 수리 · ***효과 0***
2차  애초에 «모듈»이 틀렸다(chat/tool-render 가 주인) → 재조준 · 순수 층 ✅ · ***효과 0***
3차  포매터·런타임까지 배선                          → 착지 · ***여전히 효과 0***
4차  마지막 한 칸 — index.ts:20823  foldMode: logFoldMode  ***한 줄***
```
> ### 🔑 ***매 라운드 「모양 신호」가 «참»이었다. 그리고 매 라운드 화면은 «안 바뀌었다».***
> `rg -c 'foldMode' <파일> ≥ 1` 은 ***심볼이 그 파일에 있기만 하면 참***이다 —
> 「그 값이 «흐르는가»」는 한 글자도 안 묻는다.

### ⛔ 그래서 ④ 와 ⑥ 이 «다른 물음»인 이유가 여기서 보인다

```
④ 「다른 답을 낼 수 있나」   ⇒ rg -c 는 «통과한다» (배선 안 하면 0 이 나온다)  ✅ 죽은 칸은 아니다
⑥ 「모양인가 도는가」        ⇒ rg -c 는 «실패한다» (import 만 해도 참이 된다)  🚨 여기서 걸린다
⇒ 📌 ***④ 만 보고 「판정 가능하다」로 넘기면 ⑥ 을 통째로 건너뛴다.*** 이 창이 그렇게 했다.
```

### 🩹 그래서 무엇을 쓰나 — **이 저장소에 «모범»이 있다**(2026-08-24 착지)

```ts
// src/self-dev/entrance-registry.test.ts — 진입점을 «실제로» 태운다
const { program, setRunDevAskFromGoalFileDepsForTesting } = await import('../index.js');
setRunDevAskFromGoalFileDepsForTesting({ … 무엇이 불렸는지 배열에 담는다 … });
await program.parseAsync(['node','monad','harness','ask','/tmp/goal.md']);
expect(launchedEntrances[0]).toBe(CLI_HARNESS_ASK_ENTRANCE.id);
```
⭐ 같은 파일의 **종전 판본**이 정확히 반대였다 — `expect(indexSource).toContain('…_ENTRANCE.id')`.
그 문자열 가드는 **두 방향으로 틀렸다**: 리터럴이 사라지면 성질이 멀쩡해도 빨간불이고,
***리터럴만 있고 다른 이름을 넘겨도 초록***이었다(= 막으려던 것을 못 막았다 · `OBS-T289`).

### 📌 ⇒ **⑥ 을 채우는 한 줄**

> ### ***판정 신호에 `rg -c <심볼>` 을 쓰려거든, 「그 심볼을 «부르는» 자리가 이미 테스트에 있나」를 먼저 물어라.***
> 없으면 그 신호는 ***「배선했다」가 아니라 「이름을 적었다」***를 잰다.

---

## 📏🚨⭐⭐⭐⭐ **⑦ 이 «칸»이 된 근거 — 하루에 «세 번» 같은 형태였다** (2026-08-24 · `[T]` 125차)

> 이 문서의 규칙은 **「같은 형태가 두 번이면 칸을 더한다」**다. 셋이 나왔다.

```
① [launch-branch] current branch is …, not main
   🅰️ 「내 착지 루프(브랜치 전환)가 도는 골의 전제검사와 겹쳐 중단을 만든다」
   📏 경고 있음 58런 ***53%*** / 없음 123런 ***51%***   ⇒ ❌ 차이 없음
   ⊕ 애초에 «틀린 축»이었다 — 그 경고의 목적은 실패 예측이 아니라 ***base 선택 정확성***

② `ask-launch` 축이 `harness ask` 를 못 본다 (OBS-T285)
   🅰️ 「그러니 입구 매뉴얼의 「29발 중 0발」도 이 축 오류였을 것이다」
   📏 `cli-harness-ask` 최초 관측 = 2026-08-22T13:54Z ⇒ ❌ 그 「0발」은 «쓸 때 참»이었다

③ `goal-file-lint: WARN [out-of-target-requirement]` (JDG-T65)
   🅰️ 「이 경고 때문에 자식이 «어디에 쓸지» 못 정하고 변경 0으로 끝냈다」
   📏 경고 있음 65런 ***66%*** / 없음 127런 ***42%***  ⇒ ❌ ***반대 방향***
```

> ### 🔑 ***경고를 하나 발견하면 그것으로 눈앞의 미스터리를 설명하려는 «인력»이 있다.***
> 경고는 **「거기 있다」는 이유만으로** 가장 그럴듯한 원인으로 보인다. 그리고 셋 다 ***진짜 경고***였다 —
> 문제는 경고가 틀린 게 아니라 ***내가 그것을 「이 실패의 원인」으로 «승격»시킨 것***이다.

### 🩹 그래서 이 칸의 검사는 «한 줄»이다

```bash
# 그 경고를 받은 런과 «안 받은» 런의 완주율을 센다 — 안 갈리면 원인이 아니다
grep -l '<경고 문자열>' <산출들> | …   ⇒ completed 비율
grep -L '<경고 문자열>' <산출들> | …   ⇒ completed 비율
```
⭐ 세 번 다 ***재서 반증했다.*** 규율이 없었으면 셋 다 「원인」으로 굳었을 것이다.
⛔ ⊕ **③ 의 진짜 기전은 따로 있었다**: ***「읽기만 하라」고 이름을 대도 저작기가 그 파일을 «대상»에 넣는다***
   (ask 선언 2개 → 저작된 골 5개). 그러나 그것도 «실패의 원인»으로는 **미증명**이다.

---

## 🔁 이 문서를 고쳐야 할 때
같은 형태의 실패가 **두 번** 나면 칸을 하나 «더한다». ⛔ 지우지 않는다 — 이력이 값이다.
⭐ **다만 그 형태가 «이미 있는 칸»의 것이면 칸을 더하지 «말고» 그 칸에 실물을 붙인다**(2026-08-11 · 위 §③ 실물).

## ⛔ `harness ask` 와 `harness say` 는 «다른 것»을 받는다 (2026-09-10 🅣 · 실측)

`harness ask --help` 가 말한다: **`<goal-path>`** — ***"골 문서 경로를 받아 구동한다"***.
`harness say --help`: **`<sentence...>`** — ***"문장을 받아 구동한다"***.

🩸 **이 창은 손으로 쓴 골 파일을 `ask` 에 주고 «세 번» 막혔다.** 매번 다른 얼굴로 막혀서
매번 다른 것을 고쳤고, 셋 다 진짜 원인이 아니었다:

| 시도 | 막힌 문면 | 내가 고친 것 | 실제 |
|---|---|---|---|
| ① | `(대상 경로 0)` | 1행에 `대상 경로:` 라벨을 붙였다 | ❌ 여전히 0 |
| ② | `## REQUIRED EVIDENCE 절에 … 항목이 없다` | 그 절을 손으로 썼다 | ❌ 여전히 0 |
| ③ | `(대상 경로 0)` | `- RootIntent:` 줄을 넣었다(추측) | ❌ 여전히 0 |

⭐ **진짜 이유는 lint 가 «이미 말하고 있었다»** — 나는 그 줄을 읽고 지나쳤다:
```
[dev] goal-file-lint: WARN [canonical-structure] missing required section: ## WHAT TO BUILD
[dev] goal-file-lint: WARN [canonical-structure] missing required section: ## TRACED PATHS
[dev] goal-file-lint: WARN [canonical-structure] missing required section: ## 답하지 못하는 것
[dev] goal-file-lint: WARN [canonical-structure] missing required section: ## 불변식
[dev] goal-file-markers: … recognized invariant candidates are counted only inside the
      Original ask block, but this goal file has no Original ask block.
```
⇒ ***`ask` 로 주는 골 문서는 「하니스가 저작한 것」이 전제다.*** 대상 경로도 불변식도
**`Original ask` 블록 안에서** 세어진다. 손으로 그 구조를 흉내 내는 것은 길이 아니다.

### ✅ 규율

- 손으로 쓴 문면은 **`harness say "<문면>"`** 으로 쏜다. 첫 줄에 `대상 경로: …` 라벨을 둔다.
  ⇒ 하니스가 «자기 골 문서»를 저작하고 그 안에 `Original ask` 블록을 만든다.
- 대표 지시(*"dev 골은 항상 문서로"*)는 **여전히 지킨다** — 골 원문을 `docs/goals/` 에 «기록»으로
  남기되, ⛔ **그 파일을 `ask` 의 입력으로 쓰지 않는다**. 둘은 다른 역할이다.
- `ask` 는 **하니스가 만든** 골 문서를 다시 돌릴 때(재발사·수리 라운드) 쓴다.

📏 **반증**: 손으로 쓴 파일로 `harness ask <file>` → `(대상 경로 0)` 으로 막힌다.
같은 문면을 `harness say "$(…)"` 로 → `요청문이 선언한 대상 경로 3개 · ✅ 막는 것 없음`.

⛔ **교훈은 「문을 외워라」가 아니다** — ***막힌 문면이 «두 번 같으면» 내가 고치는 축이 틀린 것이다.***
①과 ③이 **글자 그대로 같은 줄**이었는데 나는 사이에 ②를 고치느라 그걸 못 봤다.
