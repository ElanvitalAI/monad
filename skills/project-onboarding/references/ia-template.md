# {PROJECT_NAME} — Information Architecture

> Auto-generated by project-onboarding skill | {DATE}

---

## 1. Site Map / Navigation Structure

사용자가 접근 가능한 모든 페이지/화면/뷰의 계층 구조.

```mermaid
{Mermaid flowchart — 네비게이션 트리}
```

### Navigation Table

| Level | Page/View | Route/Path | Parent | Description |
|---|---|---|---|---|
| L0 | {Home} | {/} | — | {설명} |
| L1 | {Feature Page} | {/feature} | Home | {설명} |
| L2 | {Sub Feature} | {/feature/sub} | Feature Page | {설명} |

---

## 2. Content Hierarchy

각 페이지/화면에 포함된 주요 콘텐츠 블록.

### {Page 1 Name}
- {Content Block 1}: {설명}
- {Content Block 2}: {설명}

### {Page 2 Name}
- {Content Block 1}: {설명}

{... 각 페이지별 ...}

---

## 3. Data Model Overview

프로젝트의 데이터 구조 및 관계.

### Primary Data Entities

| Entity | Storage | Schema Location | Key Fields |
|---|---|---|---|
| {e.g., Company Data} | {JSON/DB/API} | {파일 경로} | {주요 필드} |

### Data Flow

```mermaid
{Mermaid flowchart — 데이터 흐름: 입력 → 처리 → 저장 → 출력}
```

### Entity Relationships

```mermaid
{Mermaid erDiagram — 엔티티 관계도 (해당되는 경우)}
```

---

## 4. Module Dependency Graph

코드 모듈 간 의존성 관계.

```mermaid
{Mermaid flowchart LR — 모듈 의존성 그래프}
```

### Module Descriptions

| Module | Directory | Responsibility | Dependencies |
|---|---|---|---|
| {e.g., Auth} | {auth/} | {인증 처리} | {config, external API} |
| {e.g., Components} | {components/} | {UI 렌더링} | {data, config} |

---

## 5. File Organization Rationale

### Directory Purpose Map

```
{project}/
+-- {dir1}/          # {역할 설명}
|   +-- {subdir}/    # {역할 설명}
+-- {dir2}/          # {역할 설명}
+-- {file}           # {역할 설명}
```

### Architecture Pattern

- **Pattern:** {MVC, Component-based, Slide-based, Layered, etc.}
- **Rationale:** {왜 이 패턴을 사용했는지 코드에서 추론}
- **Conventions:**
  - {파일명 규칙 — e.g., *_slide.py for slide components}
  - {디렉토리 규칙 — e.g., components/ for UI, data/ for data access}
