# ⚠️⛔⭐ **이 오버레이는 «오늘» 아무것도 못 바꾼다 — 그 사실을 여기 적어 둔다**(2026-09-08 실측).
#   막는 것이 «셋»이고 각각 다른 수리를 요구한다:
#     ⑴ target 이 `default-loop` 인데 ***`default-loop` 은 «활성 템플릿이 될 수 없다»***
#        (활성은 `activeTemplate` 이 고르고 그것은 `self-implement` 또는 `research-loop` 뿐이다)
#     ⑵ 조건이 `heal_attempts` 인데 실행기가 그 키를 «안 준다» ⇒ 판정은 `key-absent` 다
#     ⑶ ✅ ***열렸다 (2026-09-08 밤 · 152차 창 실측)*** — 옛 문면은 *"`maxVisits` 를 읽는 실행 코드가
#        없다 ⇒ ⑴⑵ 를 고쳐도 «관측 가능한 차이»를 못 낸다"* 였고 ***그것은 이제 거짓이다***:
#        `src/self-implement/orchestrator.ts` 의 `observeNodeEntry` 가 `visitBudgetOf(...)` 로 읽어
#        `graph-visit-budget` 을 낸다. ⇒ ⑴⑵ 를 고치면 이 패치는 ***그 이벤트의 `maxVisits` 값으로 보인다***.
#        ⛔ 다만 «막지는» 않는다(관측만) — 그리고 그 관측 자체가 `graphAuthority.enabled` 뒤에 있다.
#        🔎 반증: rg -n 'visitBudgetOf' src/self-implement/orchestrator.ts
#   🔑 ⇒ ***남은 막힘은 «둘»(⑴⑵)이다.*** 이 파일은 아직 아무것도 못 바꾸지만, 이유가 하나 줄었다.
#   ⚠️ 이 줄의 이력: 위 문면이 «같은 날 아침»에 옳았고 «그날 저녁»에 늙었다 —
#      ***「막는 것이 몇 개인가」는 인용하지 말고 그때 반증 명령으로 세라.***
#   ⛔ 시험 `test/graph-overlay-reachability.test.ts` 가 ⑴ 을 «세어» 이 상태가 조용히 늘지 않게 막는다.
#
# ⭐ 셀프힐이 「같은 실패」를 반복할 때 예산을 늘리는 «런타임» 오버레이.
#   ⛔ 「상한을 올려라」가 기본 처방이 아니다 — heal 이 `retry` 를 두 번 낸 «뒤»에만 얹는다.
#
# 🚨⛔ **경로는 «파싱된 스펙»의 키를 쓴다** — YAML 원문의 `max_visits` 가 아니라 `maxVisits` 다.
#   🩸 2026-09-08 실측: `max_visits` 로 썼더니 «없는 키»라 아무것도 안 바뀌고 `ok` 가 나왔다.
#   ⇒ 그 뒤 적용기가 `no-such-key` 로 «거절»하게 고쳤다. 그래도 여기 이름을 맞추는 것이 1차 방어다.
# ⚠️ 인덱스는 «노드 순서»에 묶인다 — default-loop 의 순서가 바뀌면 이 파일도 바뀌어야 한다.
#   (그 취약함이 JSON Patch 의 값이자 대가다. 이름 기반 경로는 다음 판의 후보다.)
overlay_id: heal-patient
target: default-loop
stage: runtime
applies_when: heal_attempts >= 2       # ⛔ 조건도 «값»이다 — 사람이 판단해 얹는 것이 아니다
patch:
  - op: replace
    path: /nodes/6/maxVisits           # rework  (⚠️ 인덱스 6 · 이름으로 확인할 것)
    value: 9
  - op: replace
    path: /nodes/7/maxVisits           # heal
    value: 5
